<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MonedasElongadas.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MonedasElongadas"
    Shell.FlyoutBehavior="Flyout"
    Title="Monedas Elongadas">

    <FlyoutItem Title="Listado" Icon="list.png">
        <ShellContent Route="MainPage" ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Nueva moneda" Icon="add.png">
        <ShellContent ContentTemplate="{DataTemplate local:NuevaMonedaPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Exportar" Icon="export.png">
        <ShellContent Route="DescargarXmlPage" ContentTemplate="{DataTemplate local:DescargarXmlPage}" />
    </FlyoutItem>
    <FlyoutItem Title="Importar" Icon="import.png">
        <ShellContent Route="ImportarXmlPage" ContentTemplate="{DataTemplate local:ImportarXmlPage}" />
    </FlyoutItem>
</Shell>
